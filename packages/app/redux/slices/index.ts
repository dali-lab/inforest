import userReducer, { UserState } from "./userSlice";
import forestReducer, { ForestState } from "./forestSlice";
import plotReducer, { PlotState } from "./plotSlice";
import teamReducer, { TeamState } from "./teamSlice";
import treeReducer, { TreeState } from "./treeSlice";
import treeLabelReducer, { TreeLabelState } from "./treeLabelSlice";
import treeSpeciesReducer, { TreeSpeciesState } from "./treeSpeciesSlice";
import treePhotoReducer, { TreePhotoState } from "./treePhotoSlice";
import treePhotoPurposeReducer, {
  TreePhotoPurposeState,
} from "./treePhotoPurposeSlice";
import forestCensusReducer, { ForestCensusState } from "./forestCensusSlice";
import plotCensusReducer, { PlotCensusState } from "./plotCensusSlice";
import treeCensusReducer, { TreeCensusState } from "./treeCensusSlice";
import treeCensusLabelReducer, {
  TreeCensusLabelState,
} from "./treeCensusLabelSlice";
import syncReducer from "./syncSlice";

export {
  userReducer,
  UserState,
  forestReducer,
  ForestState,
  plotReducer,
  PlotState,
  teamReducer,
  TeamState,
  treeReducer,
  TreeState,
  treeLabelReducer,
  TreeLabelState,
  treeSpeciesReducer,
  TreeSpeciesState,
  treePhotoReducer,
  TreePhotoState,
  treePhotoPurposeReducer,
  TreePhotoPurposeState,
  forestCensusReducer,
  ForestCensusState,
  plotCensusReducer,
  PlotCensusState,
  treeCensusReducer,
  TreeCensusState,
  treeCensusLabelReducer,
  TreeCensusLabelState,
  syncReducer,
};
